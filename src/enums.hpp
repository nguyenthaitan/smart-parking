#ifndef ENUMS_HPP
#define ENUMS_HPP

// Vehicle type enumeration
enum class VehicleType {
    MOTORCYCLE = 0,
    CAR = 1
};

// Event type enumeration
enum class EventType {
    ARRIVAL = 0,
    DEPARTURE = 1
};

#endif // ENUMS_HPP
